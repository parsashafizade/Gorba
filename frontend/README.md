# Tiny Yes frontend

React 19 + TypeScript + Vite application. Use `npm run dev` for development, `npm run test` for
unit and integration tests, and `npm run build` for the production bundle.

Runtime mascot assets are semantic copies of the canonical files in `../gorba`. Components only
read them through `src/features/mascot/assets.ts`.
